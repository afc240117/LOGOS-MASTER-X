import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.logosmaster.x',
  appName: 'LOGOS MASTER X',
  webDir: '../../app/web/static'
};

export default config;
