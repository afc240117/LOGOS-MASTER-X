# Android / Capacitor

1. Instale Node.js, JDK 17+ e Android Studio.
2. Rode `npm install`.
3. Ajuste `server.url` em `capacitor.config.ts`.
4. Rode `npx cap add android`.
5. Rode `npx cap sync android`.
6. Rode `npx cap open android`.
7. No Android Studio: Build > Build APK(s).
