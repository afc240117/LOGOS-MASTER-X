import assert from 'node:assert/strict';import {runPipeline,runCommand} from '../src/think/pipeline.js';const p=runPipeline({text:'Lamentações 5:21-22',theme:'Restauração',duration:40,intensity:3});assert.equal(p.review.status,'PASS');assert.equal(p.stages.structure.duration.points,4);assert.equal(p.stages.k7.restrictions.includes('sem glossolalia'),true);const s=runCommand('SÉRIE',{theme:'Restauração',seriesCount:5});assert.equal(s.output.episodes.length,5);console.log('Think Engine tests PASS');