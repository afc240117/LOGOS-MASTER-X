const KEY="logosx:studio:projects";export function getProjects(){try{return JSON.parse(localStorage.getItem(KEY)||"[]")}catch{return[]}}export function saveProject(project){const all=getProjects();const idx=all.findIndex(x=>x.id===project.id);if(idx>=0)all[idx]=project;else all.unshift(project);localStorage.setItem(KEY,JSON.stringify(all.slice(0,300)));return project;}export function deleteProject(id){const all=getProjects().filter(x=>x.id!==id);localStorage.setItem(KEY,JSON.stringify(all));return all;}export function getProject(id){return getProjects().find(x=>String(x.id)===String(id))||null;}
