export function studioPrompt(command,input,project){const global=window.LOGOS_BUILD_DEEP_PROMPT?window.LOGOS_BUILD_DEEP_PROMPT(command==="SERMÃO"?"sermon":"assistant",{subject:input.text||input.theme||"",duration:input.duration,cult:input.cult,audience:input.audience,intensity:input.intensity,objective:input.objective,notes:input.notes}):"";return `${global}\n\nLOGOS STUDIO COMMAND: LOGOS ${command}\n\nPIPELINE LOCAL JÁ EXECUTADO:\n${JSON.stringify(project,null,2)}\n\nINSTRUÇÃO: use o diagnóstico local como roteiro de controle, faça a pesquisa bíblica necessária e entregue o conteúdo desenvolvido. Não invente dados. Finalize com Quality Gate.`;}
