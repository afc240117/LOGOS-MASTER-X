export function seriesStage(input={}){const theme=input.theme||input.text||"Tema bíblico";const count=Math.max(2,Math.min(12,Number(input.seriesCount||4)));return {stage:"SÉRIE",title:`Série: ${theme}`,episodes:Array.from({length:count},(_,i)=>({number:i+1,title:`Mensagem ${i+1}: definir subtema textual`,goal:`Explorar um aspecto distinto de ${theme} sem repetir a mensagem anterior`,requirements:["texto próprio","grande ideia própria","aplicação própria","ligação com a série"]})),rule:"A série deve avançar, não repetir o mesmo sermão com títulos diferentes."};}
